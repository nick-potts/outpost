{
  "options": {
    "scenarios": {
      "events": {
        "executor": "constant-arrival-rate",
        "rate": 1000,
        "timeUnit": "1s",
        "duration": "100s",
        "preAllocatedVUs": 100,
        "maxVUs": 200
      }
    },
    "thresholds": {
      "http_req_duration": ["p(95)<500", "p(99)<1000"],
      "http_req_failed": ["rate<0.01"],
      "event_publish_success_rate": ["rate>=0.99"]
    }
  }
}